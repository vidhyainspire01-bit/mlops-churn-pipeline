catalog: ml_dev
schema: churn
model_name: churn_model
training:
  n_estimators: 50
  max_depth: 4
  learning_rate: 0.1
  test_size: 0.2
  random_state: 42
data:
  n_samples: 5000  # small for fast dev iteration
quality_gates:
  min_auc: 0.70
  min_precision: 0.65